{
  "name": "Node-EC2-Car-Image-Search",
  "description": "A simple NodeJs web application that searches for car images",
  "version": "0.0.1",
  "private": true,
  "devDependencies": {
    "mocha": "5.0.0",
    "unit.js": "2.0.0",
    "supertest": "3.0.0"
  },
  "scripts": {
    "test": "mocha --recursive tests --exit"
  },
  "dependencies": {
    "google-images": "^2.1.0"
  }
}
